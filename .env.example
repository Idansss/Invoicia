# App
APP_BASE_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace_me_with_a_long_random_secret

# Database
DATABASE_URL=postgresql://invoicia:invoicia@localhost:5433/invoicia?schema=public
# Optional: run tests against an isolated schema/database
# TEST_DATABASE_URL=postgresql://invoicia:invoicia@localhost:5433/invoicia?schema=test

# Redis / BullMQ
REDIS_URL=redis://localhost:6379

# Email (Mailhog for dev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Invoicia <no-reply@invoicia.local>"

# Stripe (dev/test mode)
STRIPE_PUBLIC_KEY=pk_test_replace_me
STRIPE_SECRET_KEY=sk_test_replace_me
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Optional OAuth (Google)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Storage (local dev)
STORAGE_DIR=storage

# Defaults
DEFAULT_ORG_TIMEZONE=Africa/Lagos
